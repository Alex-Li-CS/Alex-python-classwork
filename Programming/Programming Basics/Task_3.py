# a = input(the car mileage the last time the car was filled)
# b = input(the car mileage now)
# c = input(the total number of litres taken to fill the tank)
# d = b - a
# f = c / b
# ii) f as the final number would not be confusing
# iii) a,b,c,d to ensure the calculation is accurate
